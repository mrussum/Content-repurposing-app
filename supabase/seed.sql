-- Seed data for local development only
-- Run: pnpm supabase db reset

-- Test Pro user (auth.users row must be created via Supabase Auth UI or CLI)
-- After creating a user, update their profile here:
-- update public.profiles set plan = 'pro' where email = 'test@example.com';
